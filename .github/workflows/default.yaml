name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]

jobs:
  Explore-GitHub-Actions:
    environment: test-main
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo ${{ secrets.MYSECRET }} | tr 'A-Za-z' 'N-ZA-Mn-za-m'
