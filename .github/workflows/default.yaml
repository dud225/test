name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]

jobs:
  Explore-GitHub-Actions:
    environment: test-main
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo ${{ secrets.MYSECRET }} | base64 > /tmp/a
          base64 /tmp/a > /tmp/b
          base64 -d /tmp/b | base64 -d
